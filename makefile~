CC=gcc
CFLAGS=-pedantic -Wall

DEPS=pgm_process.h pgm_io.h struct.h help.h user_io.h

OBJ=imager_v3.o pgm_process.o pgm_io.o help.o user_io.o

%.o : %.c $(DEPS)
	$(CC) -c -o $@ $< $(CFLAGS)

imager_v3: $(OBJ) 
	$(CC) -o imager_v3.x $(OBJ)

clean:
	rm *.o *~

del:
	rm imager_v3.x
